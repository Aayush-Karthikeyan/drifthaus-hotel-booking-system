<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/listings" aria-label="Drifthaus home">
            <span class="brand-icon-wrap">
                <i class="fa-solid fa-wind brand-icon"></i>
            </span>
            <span class="brand-word">Drifthaus</span>
        </a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/listings">Explore</a>
            </div>
            <form class="d-flex search-form mx-auto" action="/listings" method="GET" role="search">
                <input
                    class="form-control search-input"
                    type="search"
                    name="search"
                    value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
                    placeholder="Search destinations"
                    aria-label="Search destinations"
                />
                <button class="btn search-btn" type="submit">
                    <i class="fa-solid fa-magnifying-glass"></i>
                    <span>Search</span>
                </button>
            </form>
            <div class="navbar-nav ms-auto">
                <a class="nav-link host-link" href="/listings/new">List your place</a>
                <% if (!currUser) { %>
                    <a class="nav-link fw-semibold" href="/signup">Sign up</a>
                    <a class="nav-link fw-semibold" href="/login">Log in</a>
                <% } else { %>
                    <a class="nav-link fw-semibold" href="/logout">Log out</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
